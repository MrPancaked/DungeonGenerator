using UnityEngine;

public class LingusBingus : MonoBehaviour
{
    public bool bingus;

    // Update is called once per frame
    private void Update()
    {
        if (bingus)
        {
            
        }
        
    }
}
